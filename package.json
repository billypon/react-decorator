{
  "name": "@billypon/react-decorator",
  "description": "Utilities for react",
  "version": "0.2.4",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && tsc -p .",
    "postbuild": "bin/post-build",
    "lint": "tslint -p ."
  },
  "dependencies": {
    "reflect-metadata": "0.1.13"
  },
  "devDependencies": {
    "tslint": "5.20.0",
    "typescript": "4.1.3"
  }
}
